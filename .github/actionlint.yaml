# actionlint 1.7.7 predates GitHub's hosted Intel alias. Declaring the label
# here extends its static catalogue; this is still a GitHub-hosted runner.
self-hosted-runner:
  labels:
    - macos-15-intel
