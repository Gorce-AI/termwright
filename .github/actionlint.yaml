# actionlint 1.7.7 predates these GitHub-hosted labels. Declaring them here
# extends its static catalogue; neither label represents a self-hosted runner.
self-hosted-runner:
  labels:
    - macos-15-intel
    - windows-11-arm
