# `@termwright/pty-win32-arm64`

Prebuilt ARM64 Windows binary consumed by `@termwright/pty`. This package is
selected automatically; do not import or install it directly. If it is missing
on a supported ARM64 Windows installation, Termwright fails closed rather than
switching to a weaker PTY backend.
