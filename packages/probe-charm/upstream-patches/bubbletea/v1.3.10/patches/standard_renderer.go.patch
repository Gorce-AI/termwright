--- a/standard_renderer.go
+++ b/standard_renderer.go
@@ -161,0 +162,5 @@
+	termwrightCommit := termwrightTryBeginOutputCommit()
+	if !termwrightCommit.proceed {
+		return
+	}
+	defer termwrightCommit.end()
@@ -166,0 +172 @@
+		termwrightAfterRendererFlush(r, true)
@@ -283 +289,2 @@
-	_, _ = r.out.Write(buf.Bytes())
+	written, writeErr := r.out.Write(buf.Bytes())
+	termwrightAfterRendererFlush(r, writeErr == nil && written == buf.Len())
