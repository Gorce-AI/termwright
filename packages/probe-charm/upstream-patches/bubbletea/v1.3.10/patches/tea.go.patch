--- a/tea.go
+++ b/tea.go
@@ -502 +502,2 @@
-			p.renderer.write(model.View()) // send view to renderer
+			// Injected by termwright: the loop's frame.
+			termwrightRenderAndObserve(p, model)
@@ -578,0 +580,2 @@
+	termwrightRunProbe := termwrightBeforeRun()
+	defer termwrightShutdown(termwrightRunProbe)
@@ -700 +703,2 @@
-	p.renderer.write(model.View())
+	// Injected by termwright: the first frame.
+	termwrightRenderAndObserve(p, model)
@@ -739 +743,2 @@
-		p.renderer.write(model.View())
+		// Injected by termwright: the last frame.
+		termwrightRenderAndObserve(p, model)
