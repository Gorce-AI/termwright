# `@termwright/pty-linux-x64`

Prebuilt x64 glibc Linux binary selected automatically by `@termwright/pty`. Do
not import or install it directly. If it is missing, Termwright fails closed
rather than switching to a weaker PTY backend.
