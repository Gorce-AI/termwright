diff --git a/src/host/VtInputThread.cpp b/src/host/VtInputThread.cpp
index d89dd4c..d59c448 100644
--- a/src/host/VtInputThread.cpp
+++ b/src/host/VtInputThread.cpp
@@ -12,6 +12,12 @@
 #include "../terminal/parser/InputStateMachineEngine.hpp"
 #include "../types/inc/utils.hpp"

+#include <bcrypt.h>
+
+#include <array>
+
+#pragma comment(lib, "bcrypt.lib")
+
 using namespace Microsoft::Console;
 using namespace Microsoft::Console::Interactivity;
 using namespace Microsoft::Console::VirtualTerminal;
@@ -188,10 +194,48 @@ void VtInputThread::_InputThread()
     return S_OK;
 }

-void VtInputThread::CaptureNextCursorPositionReport() const noexcept
+std::optional<std::string> VtInputThread::BeginHostCursorPositionRequest(const uintptr_t target, const uint32_t generation, std::shared_ptr<void> synchronization) const
+{
+    for (;;)
+    {
+        std::array<unsigned char, 16> random{};
+        if (!BCRYPT_SUCCESS(BCryptGenRandom(nullptr, random.data(), gsl::narrow_cast<ULONG>(random.size()), BCRYPT_USE_SYSTEM_PREFERRED_RNG)))
+        {
+            return std::nullopt;
+        }
+
+        static constexpr char hex[] = "0123456789abcdef";
+        std::string token(random.size() * 2, '\0');
+        for (size_t i = 0; i < random.size(); ++i)
+        {
+            token[i * 2] = hex[random[i] >> 4];
+            token[i * 2 + 1] = hex[random[i] & 0xf];
+        }
+
+        auto& engine = static_cast<InputStateMachineEngine&>(_pInputStateMachine->Engine());
+        if (engine.RegisterHostCursorPositionRequest(std::wstring{ token.begin(), token.end() }, target, generation, synchronization))
+        {
+            return token;
+        }
+    }
+}
+
+void VtInputThread::CancelHostCursorPositionRequest() const noexcept
+{
+    auto& engine = static_cast<InputStateMachineEngine&>(_pInputStateMachine->Engine());
+    engine.CancelHostCursorPositionRequest();
+}
+
+bool VtInputThread::WaitUntilStartupHostCursorPositionRequestCompletes() const noexcept
+{
+    auto& engine = static_cast<InputStateMachineEngine&>(_pInputStateMachine->Engine());
+    return engine.WaitUntilStartupHostCursorPositionRequestCompletes();
+}
+
+void VtInputThread::InvalidateHostCursorPositionRequests(const uintptr_t target) const noexcept
 {
     auto& engine = static_cast<InputStateMachineEngine&>(_pInputStateMachine->Engine());
-    engine.CaptureNextCursorPositionReport();
+    engine.InvalidateHostCursorPositionRequests(target);
 }

 til::enumset<DeviceAttribute, uint64_t> VtInputThread::WaitUntilDA1(DWORD timeout) const noexcept
diff --git a/src/host/VtInputThread.hpp b/src/host/VtInputThread.hpp
index 09c0f89..eb2ee86 100644
--- a/src/host/VtInputThread.hpp
+++ b/src/host/VtInputThread.hpp
@@ -16,6 +16,9 @@ Author(s):

 #include "../terminal/parser/StateMachine.hpp"

+#include <optional>
+#include <string>
+
 namespace Microsoft::Console
 {
     namespace VirtualTerminal
@@ -29,7 +32,10 @@ namespace Microsoft::Console
         explicit VtInputThread(_In_ wil::unique_hfile hPipe);

         [[nodiscard]] HRESULT Start();
-        void CaptureNextCursorPositionReport() const noexcept;
+        std::optional<std::string> BeginHostCursorPositionRequest(uintptr_t target, uint32_t generation, std::shared_ptr<void> synchronization) const;
+        void InvalidateHostCursorPositionRequests(uintptr_t target) const noexcept;
+        void CancelHostCursorPositionRequest() const noexcept;
+        bool WaitUntilStartupHostCursorPositionRequestCompletes() const noexcept;
         til::enumset<VirtualTerminal::DeviceAttribute, uint64_t> WaitUntilDA1(DWORD timeout) const noexcept;

     private:
diff --git a/src/host/VtIo.cpp b/src/host/VtIo.cpp
index f5214d3..9076134 100644
--- a/src/host/VtIo.cpp
+++ b/src/host/VtIo.cpp
@@ -190,7 +190,11 @@ bool VtIo::IsUsingVt() const
             // wait for the DA1 response below and effectively wait for both.
             if (_lookingForCursorPosition)
             {
-                writer.WriteDSRCPR();
+                if (!writer.WriteDSRCPR())
+                {
+                    _state = State::StartupFailed;
+                    return E_UNEXPECTED;
+                }
             }

             // GH#4999 - Send a sequence to the connected terminal to request
@@ -211,6 +215,11 @@ bool VtIo::IsUsingVt() const
             auto& gci = ServiceLocator::LocateGlobals().getConsoleInformation();
             const auto suspension = gci.SuspendLock();
             _deviceAttributes = _pVtInputThread->WaitUntilDA1(3000);
+            if (_lookingForCursorPosition && !_pVtInputThread->WaitUntilStartupHostCursorPositionRequestCompletes())
+            {
+                _state = State::StartupFailed;
+                return E_ABORT;
+            }
         }
     }

@@ -299,6 +308,14 @@ void VtIo::SendCloseEvent()
     LockConsole();
     const auto unlock = wil::scope_exit([] { UnlockConsole(); });

+    CancelHostCursorPositionRequest();
+    auto& gci = ServiceLocator::LocateGlobals().getConsoleInformation();
+    for (auto* screen = gci.ScreenBuffers; screen != nullptr; screen = screen->Next)
+    {
+        screen->CancelConptyCursorPositionSynchronization();
+        screen->GetActiveBuffer().CancelConptyCursorPositionSynchronization();
+    }
+
     // If we're still in the process of starting up, and we're asked to shut down
     // (broken pipe), `VtIo::StartIfNeeded()` will handle the cleanup for us.
     // This can happen during the call to `WaitUntilDA1`, because we relinquish
@@ -318,6 +335,22 @@ void VtIo::SendCloseEvent()
     }
 }

+void VtIo::CancelHostCursorPositionRequest() noexcept
+{
+    if (_pVtInputThread)
+    {
+        _pVtInputThread->CancelHostCursorPositionRequest();
+    }
+}
+
+void VtIo::InvalidateHostCursorPositionRequests(SCREEN_INFORMATION* const target) noexcept
+{
+    if (_pVtInputThread)
+    {
+        _pVtInputThread->InvalidateHostCursorPositionRequests(reinterpret_cast<uintptr_t>(target));
+    }
+}
+
 // Returns true for C0 characters and C1 [single-character] CSI.
 // A copy of isActionableFromGround() from stateMachine.cpp.
 static constexpr bool IsControlCharacter(wchar_t wch) noexcept
@@ -734,15 +767,22 @@ void VtIo::Writer::WriteASB(bool enabled) const
 }

 // DSR CPR: Cursor Position Report
-bool VtIo::Writer::WriteDSRCPR() const
+bool VtIo::Writer::WriteDSRCPR(SCREEN_INFORMATION* const target, const uint32_t generation, std::shared_ptr<void> synchronization) const
 {
     if (!_io->_pVtInputThread)
     {
         return false;
     }

-    _io->_back.append("\x1b[6n");
-    _io->_pVtInputThread->CaptureNextCursorPositionReport();
+    const auto token = _io->_pVtInputThread->BeginHostCursorPositionRequest(reinterpret_cast<uintptr_t>(target), generation, std::move(synchronization));
+    if (!token)
+    {
+        return false;
+    }
+
+    _io->_back.append("\x1b]8488;twh-cpr-v1:q:");
+    _io->_back.append(*token);
+    _io->_back.push_back('\x07');
     return true;
 }

diff --git a/src/host/VtIo.hpp b/src/host/VtIo.hpp
index 7330bda..2a31642 100644
--- a/src/host/VtIo.hpp
+++ b/src/host/VtIo.hpp
@@ -6,7 +6,10 @@
 #include "VtInputThread.hpp"
 #include "PtySignalInputThread.hpp"

+#include <memory>
+
 class ConsoleArguments;
+class SCREEN_INFORMATION;

 namespace Microsoft::Console::VirtualTerminal
 {
@@ -40,7 +43,7 @@ namespace Microsoft::Console::VirtualTerminal
             void WriteSGR1006(bool enabled) const;
             void WriteDECAWM(bool enabled) const;
             void WriteASB(bool enabled) const;
-            bool WriteDSRCPR() const;
+            bool WriteDSRCPR(SCREEN_INFORMATION* target = nullptr, uint32_t generation = 0, std::shared_ptr<void> synchronization = {}) const;
             void WriteWindowVisibility(bool visible) const;
             void WriteWindowTitle(std::wstring_view title) const;
             void WriteAttributes(const TextAttribute& attributes) const;
@@ -65,6 +68,8 @@ namespace Microsoft::Console::VirtualTerminal
         void SetDeviceAttributes(til::enumset<DeviceAttribute, uint64_t> attributes) noexcept;
         til::enumset<DeviceAttribute, uint64_t> GetDeviceAttributes() const noexcept;
         void SendCloseEvent();
+        void CancelHostCursorPositionRequest() noexcept;
+        void InvalidateHostCursorPositionRequests(SCREEN_INFORMATION* target) noexcept;
         void CreatePseudoWindow();

     private:
diff --git a/src/host/screenInfo.cpp b/src/host/screenInfo.cpp
index b037275..ca5fa91 100644
--- a/src/host/screenInfo.cpp
+++ b/src/host/screenInfo.cpp
@@ -47,6 +47,12 @@ SCREEN_INFORMATION::SCREEN_INFORMATION(
 // - console handle table lock must be held when calling this routine
 SCREEN_INFORMATION::~SCREEN_INFORMATION()
 {
+    auto& gci = ServiceLocator::LocateGlobals().getConsoleInformation();
+    if (gci.IsInVtIoMode())
+    {
+        gci.GetVtIo()->InvalidateHostCursorPositionRequests(this);
+        CancelConptyCursorPositionSynchronization();
+    }
     _FreeOutputStateMachine();
 }

@@ -1416,7 +1422,17 @@ NT_CATCH_RETURN()
 // This fixes some of the most glaring out of sync issues. See GH#18725.
 bool SCREEN_INFORMATION::ConptyCursorPositionMayBeWrong() const noexcept
 {
-    return _conptyCursorPositionMayBeWrong.load(std::memory_order_relaxed);
+    return (_conptyCursorPositionSynchronization->generation.load(std::memory_order_relaxed) & 1) != 0;
+}
+
+uint32_t SCREEN_INFORMATION::ConptyCursorPositionGeneration() const noexcept
+{
+    return _conptyCursorPositionSynchronization->generation.load(std::memory_order_relaxed);
+}
+
+std::shared_ptr<ConptyCursorPositionSynchronization> SCREEN_INFORMATION::GetConptyCursorPositionSynchronization() const noexcept
+{
+    return _conptyCursorPositionSynchronization;
 }

 // This should be called whenever we do something that may desynchronize
@@ -1429,7 +1445,18 @@ void SCREEN_INFORMATION::SetConptyCursorPositionMayBeWrong() noexcept

     if (gci.IsInVtIoMode())
     {
-        _conptyCursorPositionMayBeWrong.store(true, std::memory_order_relaxed);
+        const auto& synchronization = _conptyCursorPositionSynchronization;
+        if (synchronization->canceled.load(std::memory_order_relaxed))
+        {
+            return;
+        }
+        gci.GetVtIo()->InvalidateHostCursorPositionRequests(this);
+        auto generation = synchronization->generation.load(std::memory_order_relaxed);
+        while (!synchronization->generation.compare_exchange_weak(generation, (generation | 1) + 2, std::memory_order_relaxed))
+        {
+        }
+        til::atomic_notify_all(synchronization->generation);
+        gci.OutputQueue.NotifyWaiters(true);
     }
 }

@@ -1437,56 +1464,67 @@ void SCREEN_INFORMATION::SetConptyCursorPositionMayBeWrong() noexcept
 // See ConptyCursorPositionMayBeWrong().
 void SCREEN_INFORMATION::ResetConptyCursorPositionMayBeWrong() noexcept
 {
-    _conptyCursorPositionMayBeWrong.store(false, std::memory_order_relaxed);
-    til::atomic_notify_all(_conptyCursorPositionMayBeWrong);
+    const auto& synchronization = _conptyCursorPositionSynchronization;
+    auto generation = synchronization->generation.load(std::memory_order_relaxed);
+    if ((generation & 1) != 0 && synchronization->generation.compare_exchange_strong(generation, generation + 1, std::memory_order_relaxed))
+    {
+        til::atomic_notify_all(synchronization->generation);
+        auto& gci = ServiceLocator::LocateGlobals().getConsoleInformation();
+        gci.OutputQueue.NotifyWaiters(true);
+    }
+}
+
+void SCREEN_INFORMATION::CancelConptyCursorPositionSynchronization() noexcept
+{
+    _conptyCursorPositionSynchronization->canceled.store(true, std::memory_order_relaxed);
+    ResetConptyCursorPositionMayBeWrong();
+    til::atomic_notify_all(_conptyCursorPositionSynchronization->generation);
+    auto& gci = ServiceLocator::LocateGlobals().getConsoleInformation();
+    gci.OutputQueue.NotifyWaiters(true);
+}
+
+void SCREEN_INFORMATION::ResetConptyCursorPositionGeneration(const uint32_t generation) noexcept
+{
+    const auto& synchronization = _conptyCursorPositionSynchronization;
+    auto expected = generation;
+    if ((generation & 1) != 0 && synchronization->generation.compare_exchange_strong(expected, generation + 1, std::memory_order_relaxed))
+    {
+        til::atomic_notify_all(synchronization->generation);
+        auto& gci = ServiceLocator::LocateGlobals().getConsoleInformation();
+        gci.OutputQueue.NotifyWaiters(true);
+    }
 }

 // Call this to synchronously wait until the ConPTY cursor position
 // is known to be correct again. To do so, this emits a DSR CPR sequence
 // and waits for a response from the terminal.
 // See ConptyCursorPositionMayBeWrong().
-void SCREEN_INFORMATION::WaitForConptyCursorPositionToBeSynchronized() noexcept
+HRESULT SCREEN_INFORMATION::EnsureConptyCursorPositionRequest(SCREEN_INFORMATION* const target, const std::shared_ptr<ConptyCursorPositionSynchronization>& synchronization) noexcept
 {
-    if (!_conptyCursorPositionMayBeWrong.load(std::memory_order_relaxed))
-    {
-        return;
-    }
-
     auto& gci = ServiceLocator::LocateGlobals().getConsoleInformation();
+    FAIL_FAST_IF(!gci.IsConsoleLocked());

+    if (synchronization->canceled.load(std::memory_order_relaxed))
     {
-        gci.LockConsole();
-        const auto exit = wil::scope_exit([&] { gci.UnlockConsole(); });
-        auto writer = gci.GetVtWriterForBuffer(this);
-
-        if (!writer || !writer.WriteDSRCPR())
-        {
-            _conptyCursorPositionMayBeWrong.store(false, std::memory_order_relaxed);
-            return;
-        }
-
-        writer.Submit();
+        return S_OK;
     }

-    // If you were to hold the lock, the ConPTY input thread couldn't
-    // process any input and thus couldn't update the cursor position.
-    assert(!gci.IsConsoleLocked());
-
-    for (;;)
+    const auto generation = synchronization->generation.load(std::memory_order_relaxed);
+    const auto previousGeneration = synchronization->requestedGeneration.exchange(generation, std::memory_order_relaxed);
+    if ((generation & 1) == 0 || previousGeneration == generation)
     {
-        if (!_conptyCursorPositionMayBeWrong.load(std::memory_order::relaxed))
-        {
-            break;
-        }
+        return S_OK;
+    }

-        // atomic_wait() returns false when the timeout expires.
-        // Technically we should decrement the timeout with each iteration,
-        // but I suspect infinite spurious wake-ups are a theoretical problem.
-        if (!til::atomic_wait(_conptyCursorPositionMayBeWrong, true, 500))
-        {
-            break;
-        }
+    auto writer = gci.GetVtWriterForBuffer(target);
+    if (!writer || !writer.WriteDSRCPR(target, generation, synchronization))
+    {
+        auto expected = generation;
+        synchronization->requestedGeneration.compare_exchange_strong(expected, previousGeneration, std::memory_order_relaxed);
+        return E_UNEXPECTED;
     }
+    writer.Submit();
+    return S_OK;
 }

 // Routine Description:
diff --git a/src/host/screenInfo.hpp b/src/host/screenInfo.hpp
index 7f29af8..4499d60 100644
--- a/src/host/screenInfo.hpp
+++ b/src/host/screenInfo.hpp
@@ -26,8 +26,17 @@ Revision History:
 #include "../renderer/inc/FontInfoDesired.hpp"
 #include "../server/ObjectHeader.h"

+#include <memory>
+
 class ConversionAreaInfo; // forward decl window. circular reference

+struct ConptyCursorPositionSynchronization
+{
+    std::atomic<uint32_t> generation{ 0 };
+    std::atomic<uint32_t> requestedGeneration{ 0 };
+    std::atomic<bool> canceled{ false };
+};
+
 class SCREEN_INFORMATION : public ConsoleObjectHeader, public Microsoft::Console::IIoProvider
 {
 public:
@@ -160,9 +169,13 @@ public:
     Microsoft::Console::Types::Viewport GetTerminalBufferSize() const;
     bool SendNotifyBeep() const;
     bool ConptyCursorPositionMayBeWrong() const noexcept;
+    uint32_t ConptyCursorPositionGeneration() const noexcept;
+    std::shared_ptr<ConptyCursorPositionSynchronization> GetConptyCursorPositionSynchronization() const noexcept;
     void SetConptyCursorPositionMayBeWrong() noexcept;
     void ResetConptyCursorPositionMayBeWrong() noexcept;
-    void WaitForConptyCursorPositionToBeSynchronized() noexcept;
+    void ResetConptyCursorPositionGeneration(uint32_t generation) noexcept;
+    void CancelConptyCursorPositionSynchronization() noexcept;
+    static HRESULT EnsureConptyCursorPositionRequest(SCREEN_INFORMATION* target, const std::shared_ptr<ConptyCursorPositionSynchronization>& synchronization) noexcept;

     DWORD OutputMode = ENABLE_PROCESSED_OUTPUT | ENABLE_WRAP_AT_EOL_OUTPUT;
     short WheelDelta = 0;
@@ -219,7 +232,9 @@ private:
     //  the viewport to move (SetBufferInfo, WriteConsole, etc)
     til::CoordType _virtualBottom = 0;
     std::optional<til::size> _deferredPtyResize;
-    std::atomic<bool> _conptyCursorPositionMayBeWrong = false;
+    // A waiter retains this state after releasing the console lock, so deleting
+    // an alternate screen buffer cannot invalidate the atomic it sleeps on.
+    std::shared_ptr<ConptyCursorPositionSynchronization> _conptyCursorPositionSynchronization{ std::make_shared<ConptyCursorPositionSynchronization>() };

 #ifdef UNIT_TESTING
     friend class TextBufferIteratorTests;
diff --git a/src/server/ApiDispatchers.cpp b/src/server/ApiDispatchers.cpp
index fc7d83d..1c8791a 100644
--- a/src/server/ApiDispatchers.cpp
+++ b/src/server/ApiDispatchers.cpp
@@ -10,6 +10,9 @@
 #include "../host/stream.h"
 #include "../host/srvinit.h"
 #include "../host/cmdline.h"
+#include "../host/handle.h"
+#include "../host/screenInfo.hpp"
+#include "WaitQueue.h"

 // Assumes that it will find <m> in the calling environment.
 #define TraceConsoleAPICallWithOrigin(ApiName, ...)                  \
@@ -43,6 +46,99 @@ constexpr T saturate(auto val)
     return val < min ? min : (val > max ? max : val);
 }

+namespace
+{
+    void fillScreenBufferInfoReply(CONSOLE_SCREENBUFFERINFO_MSG& reply, const CONSOLE_SCREEN_BUFFER_INFOEX& info) noexcept
+    {
+        reply.FullscreenSupported = !!info.bFullscreenSupported;
+        CopyMemory(reply.ColorTable, info.ColorTable, sizeof(reply.ColorTable));
+        reply.CursorPosition = info.dwCursorPosition;
+        reply.MaximumWindowSize = info.dwMaximumWindowSize;
+        reply.Size = info.dwSize;
+        reply.ScrollPosition.X = info.srWindow.Left;
+        reply.ScrollPosition.Y = info.srWindow.Top;
+        reply.CurrentWindowSize.X = info.srWindow.Right - info.srWindow.Left;
+        reply.CurrentWindowSize.Y = info.srWindow.Bottom - info.srWindow.Top;
+        reply.Attributes = info.wAttributes;
+        reply.PopupAttributes = info.wPopupAttributes;
+    }
+
+    class ConptyScreenBufferInfoWait final : public IWaitRoutine
+    {
+    public:
+        ConptyScreenBufferInfoWait(IApiRoutines* const api,
+                                   SCREEN_INFORMATION* const screenBuffer,
+                                   SCREEN_INFORMATION* const target,
+                                   std::shared_ptr<ConptyCursorPositionSynchronization> synchronization) :
+            IWaitRoutine(ReplyDataType::Read),
+            _api{ api },
+            _screenBuffer{ screenBuffer },
+            _target{ target },
+            _synchronization{ std::move(synchronization) }
+        {
+        }
+
+        const SCREEN_INFORMATION* GetScreenBuffer() const noexcept override
+        {
+            return _screenBuffer;
+        }
+
+        void MigrateUserBuffersOnTransitionToBackgroundWait(const void*, void*) override
+        {
+        }
+
+        bool Notify(const WaitTerminationReason reason,
+                    const bool,
+                    NTSTATUS* const replyStatus,
+                    size_t* const numBytes,
+                    DWORD* const controlKeyState,
+                    void* const outputData) override
+        {
+            FAIL_FAST_IF_NULL(replyStatus);
+            FAIL_FAST_IF_NULL(numBytes);
+            FAIL_FAST_IF_NULL(controlKeyState);
+            FAIL_FAST_IF_NULL(outputData);
+
+            *numBytes = 0;
+            *controlKeyState = 0;
+            if (WI_IsFlagSet(reason, WaitTerminationReason::ThreadDying))
+            {
+                *replyStatus = STATUS_THREAD_IS_TERMINATING;
+                return true;
+            }
+            if (WI_IsFlagSet(reason, WaitTerminationReason::HandleClosing))
+            {
+                *replyStatus = STATUS_ALERTED;
+                return true;
+            }
+
+            const auto generation = _synchronization->generation.load(std::memory_order_relaxed);
+            if (!_synchronization->canceled.load(std::memory_order_relaxed) && (generation & 1) != 0)
+            {
+                const auto hr = SCREEN_INFORMATION::EnsureConptyCursorPositionRequest(_target, _synchronization);
+                if (FAILED(hr))
+                {
+                    *replyStatus = NTSTATUS_FROM_HRESULT(hr);
+                    return true;
+                }
+                return false;
+            }
+
+            auto& info = static_cast<ConsoleScreenBufferInfoWaitReply*>(outputData)->value;
+            info.cbSize = sizeof(info);
+            _api->GetConsoleScreenBufferInfoExImpl(*_screenBuffer, info);
+            *replyStatus = STATUS_SUCCESS;
+            return true;
+        }
+
+    private:
+        IApiRoutines* const _api;
+        SCREEN_INFORMATION* const _screenBuffer;
+        SCREEN_INFORMATION* const _target;
+        const std::shared_ptr<ConptyCursorPositionSynchronization> _synchronization;
+    };
+}
+
 [[nodiscard]] HRESULT ApiDispatchers::ServerGetConsoleCP(_Inout_ CONSOLE_API_MSG* const m,
                                                          _Inout_ BOOL* const /*pbReplyPending*/)
 {
@@ -581,8 +677,9 @@ constexpr T saturate(auto val)
 }

 [[nodiscard]] HRESULT ApiDispatchers::ServerGetConsoleScreenBufferInfo(_Inout_ CONSOLE_API_MSG* const m,
-                                                                       _Inout_ BOOL* const /*pbReplyPending*/)
+                                                                       _Inout_ BOOL* const pbReplyPending)
 {
+    *pbReplyPending = FALSE;
     const auto a = &m->u.consoleMsgL2.GetConsoleScreenBufferInfo;

     CONSOLE_SCREEN_BUFFER_INFOEX ex = { 0 };
@@ -594,29 +691,34 @@ constexpr T saturate(auto val)
     SCREEN_INFORMATION* pObj;
     RETURN_IF_FAILED(pObjectHandle->GetScreenBuffer(GENERIC_READ, &pObj));

-    // See ConptyCursorPositionMayBeWrong() for details.
-    auto& activeBuffer = pObj->GetActiveBuffer();
-    // GetConsoleScreenBufferInfoExImpl uses GetActiveBuffer internally, but
-    // under the console lock.
-    if (activeBuffer.ConptyCursorPositionMayBeWrong())
+    // Capture a lifetime-safe synchronization object while the selected
+    // active buffer is protected by the console lock. The alternate buffer
+    // may be removed while the causal host query is in flight.
+    SCREEN_INFORMATION* activeBuffer = nullptr;
+    std::shared_ptr<ConptyCursorPositionSynchronization> synchronization;
+    auto replyPending = false;
+    {
+        LockConsole();
+        const auto unlock = wil::scope_exit([] { UnlockConsole(); });
+        activeBuffer = &pObj->GetActiveBuffer();
+        synchronization = activeBuffer->GetConptyCursorPositionSynchronization();
+        if (!synchronization->canceled.load(std::memory_order_relaxed) && (synchronization->generation.load(std::memory_order_relaxed) & 1) != 0)
+        {
+            RETURN_IF_FAILED(SCREEN_INFORMATION::EnsureConptyCursorPositionRequest(activeBuffer, synchronization));
+            RETURN_IF_FAILED(ConsoleWaitQueue::s_CreateWait(
+                m,
+                new ConptyScreenBufferInfoWait{ m->_pApiRoutines, pObj, activeBuffer, synchronization }));
+            replyPending = true;
+        }
+    }
+    if (replyPending)
     {
-        activeBuffer.WaitForConptyCursorPositionToBeSynchronized();
+        *pbReplyPending = TRUE;
+        return S_OK;
     }

     m->_pApiRoutines->GetConsoleScreenBufferInfoExImpl(*pObj, ex);
-
-    a->FullscreenSupported = !!ex.bFullscreenSupported;
-    const auto ColorTableSizeInBytes = RTL_NUMBER_OF_V2(ex.ColorTable) * sizeof(*ex.ColorTable);
-    CopyMemory(a->ColorTable, ex.ColorTable, ColorTableSizeInBytes);
-    a->CursorPosition = ex.dwCursorPosition;
-    a->MaximumWindowSize = ex.dwMaximumWindowSize;
-    a->Size = ex.dwSize;
-    a->ScrollPosition.X = ex.srWindow.Left;
-    a->ScrollPosition.Y = ex.srWindow.Top;
-    a->CurrentWindowSize.X = ex.srWindow.Right - ex.srWindow.Left;
-    a->CurrentWindowSize.Y = ex.srWindow.Bottom - ex.srWindow.Top;
-    a->Attributes = ex.wAttributes;
-    a->PopupAttributes = ex.wPopupAttributes;
+    fillScreenBufferInfoReply(*a, ex);

     TraceConsoleAPICallWithOrigin(
         "GetConsoleScreenBufferInfo",
diff --git a/src/server/ApiSorter.h b/src/server/ApiSorter.h
index 6366b28..0920896 100644
--- a/src/server/ApiSorter.h
+++ b/src/server/ApiSorter.h
@@ -26,9 +26,10 @@ typedef HRESULT (*PCONSOLE_API_ROUTINE)(_Inout_ PCONSOLE_API_MSG m, _Inout_ PBOO
 // 0x01 stands for level 1 API (layers are 1-based)
 // 0x000004 stands for the 5th one down in the layer structure (call IDs are 0-based)
 // clang-format off
 #define API_NUMBER_GETCONSOLEINPUT 0x01000004
 #define API_NUMBER_READCONSOLE     0x01000005
 #define API_NUMBER_WRITECONSOLE    0x01000006
+#define API_NUMBER_GETCONSOLESCREENBUFFERINFO 0x02000007
 // clang-format on

 class ApiSorter
diff --git a/src/server/IWaitRoutine.h b/src/server/IWaitRoutine.h
index b213c26..2ed50ac 100644
--- a/src/server/IWaitRoutine.h
+++ b/src/server/IWaitRoutine.h
@@ -26,6 +26,11 @@ enum class ReplyDataType
     Read = 2,
 };

+struct ConsoleScreenBufferInfoWaitReply
+{
+    CONSOLE_SCREEN_BUFFER_INFOEX value{};
+};
+
 class IWaitRoutine
 {
 public:
diff --git a/src/server/WaitBlock.cpp b/src/server/WaitBlock.cpp
index aed40e1..6126c3c 100644
--- a/src/server/WaitBlock.cpp
+++ b/src/server/WaitBlock.cpp
@@ -150,6 +150,7 @@ bool ConsoleWaitBlock::Notify(const WaitTerminationReason TerminationReason)
     auto fIsUnicode = true;

     InputEventQueue outEvents;
+    ConsoleScreenBufferInfoWaitReply screenBufferInfo;
     // TODO: MSFT 14104228 - get rid of this void* and get the data
     // out of the read wait object properly.
     void* pOutputData = nullptr;
@@ -177,6 +178,11 @@ bool ConsoleWaitBlock::Notify(const WaitTerminationReason TerminationReason)
         fIsUnicode = !!a->Unicode;
         break;
     }
+    case API_NUMBER_GETCONSOLESCREENBUFFERINFO:
+    {
+        pOutputData = &screenBufferInfo;
+        break;
+    }
     default:
     {
         FAIL_FAST_HR(E_NOTIMPL); // we shouldn't be getting a wait/notify on API numbers we don't support.
@@ -236,6 +242,22 @@ bool ConsoleWaitBlock::Notify(const WaitTerminationReason TerminationReason)
             auto a = &(_WaitReplyMessage.u.consoleMsgL1.WriteConsoleW);
             a->NumBytes = gsl::narrow<ULONG>(NumBytes);
         }
+        else if (API_NUMBER_GETCONSOLESCREENBUFFERINFO == _WaitReplyMessage.msgHeader.ApiNumber)
+        {
+            const auto& info = screenBufferInfo.value;
+            auto& reply = _WaitReplyMessage.u.consoleMsgL2.GetConsoleScreenBufferInfo;
+            reply.FullscreenSupported = !!info.bFullscreenSupported;
+            CopyMemory(reply.ColorTable, info.ColorTable, sizeof(reply.ColorTable));
+            reply.CursorPosition = info.dwCursorPosition;
+            reply.MaximumWindowSize = info.dwMaximumWindowSize;
+            reply.Size = info.dwSize;
+            reply.ScrollPosition.X = info.srWindow.Left;
+            reply.ScrollPosition.Y = info.srWindow.Top;
+            reply.CurrentWindowSize.X = info.srWindow.Right - info.srWindow.Left;
+            reply.CurrentWindowSize.Y = info.srWindow.Bottom - info.srWindow.Top;
+            reply.Attributes = info.wAttributes;
+            reply.PopupAttributes = info.wPopupAttributes;
+        }

         _WaitReplyMessage.ReleaseMessageBuffers();

diff --git a/src/terminal/adapter/IInteractDispatch.hpp b/src/terminal/adapter/IInteractDispatch.hpp
index b8aba0f..5834124 100644
--- a/src/terminal/adapter/IInteractDispatch.hpp
+++ b/src/terminal/adapter/IInteractDispatch.hpp
@@ -18,6 +18,8 @@ Author(s):
 #include "DispatchTypes.hpp"
 #include "../../types/inc/IInputEvent.hpp"

+#include <memory>
+
 namespace Microsoft::Console::VirtualTerminal
 {
     class IInteractDispatch
@@ -36,6 +38,7 @@ namespace Microsoft::Console::VirtualTerminal
         virtual void WriteStringRaw(std::wstring_view string) = 0;
         virtual void WindowManipulation(DispatchTypes::WindowManipulationType function, VTParameter parameter1, VTParameter parameter2) = 0;
         virtual void MoveCursor(VTInt row, VTInt col) = 0;
+        virtual void MoveCursorForHostRequest(VTInt, VTInt, uintptr_t, uint32_t, const std::shared_ptr<void>&) {}
         virtual void FocusChanged(bool focused) = 0;
     };
 }
diff --git a/src/terminal/adapter/InteractDispatch.cpp b/src/terminal/adapter/InteractDispatch.cpp
index 8c64743..76f7bd0 100644
--- a/src/terminal/adapter/InteractDispatch.cpp
+++ b/src/terminal/adapter/InteractDispatch.cpp
@@ -169,6 +169,34 @@ void InteractDispatch::MoveCursor(const VTInt row, const VTInt col)
     info.GetActiveBuffer().ResetConptyCursorPositionMayBeWrong();
 }

+void InteractDispatch::MoveCursorForHostRequest(const VTInt row, const VTInt col, const uintptr_t target, const uint32_t generation, const std::shared_ptr<void>& opaqueSynchronization)
+{
+    if (target == 0)
+    {
+        MoveCursor(row, col);
+        return;
+    }
+
+    const auto synchronization = std::static_pointer_cast<ConptyCursorPositionSynchronization>(opaqueSynchronization);
+    if (!synchronization || synchronization->canceled.load(std::memory_order_relaxed) || synchronization->generation.load(std::memory_order_relaxed) != generation)
+    {
+        return;
+    }
+
+    auto& info = *reinterpret_cast<SCREEN_INFORMATION*>(target);
+    const auto viewport = info.GetViewport();
+    til::point cursor{ col - 1 + viewport.Left(), row - 1 + viewport.Top() };
+    cursor.y = std::clamp(cursor.y, viewport.Top(), viewport.BottomInclusive());
+    cursor.x = std::clamp(cursor.x, viewport.Left(), viewport.RightInclusive());
+    const auto& api = ServiceLocator::LocateGlobals().api;
+    const auto result = api->SetConsoleCursorPositionImpl(info, cursor);
+    if (FAILED(result))
+    {
+        FAIL_FAST_HR(result);
+    }
+    info.ResetConptyCursorPositionGeneration(generation);
+}
+
 // Routine Description:
 // - Checks if the InputBuffer is willing to accept VT Input directly
 // Arguments:
diff --git a/src/terminal/adapter/InteractDispatch.hpp b/src/terminal/adapter/InteractDispatch.hpp
index 2fe63b0..5148aec 100644
--- a/src/terminal/adapter/InteractDispatch.hpp
+++ b/src/terminal/adapter/InteractDispatch.hpp
@@ -33,6 +33,7 @@ namespace Microsoft::Console::VirtualTerminal
         void WriteStringRaw(std::wstring_view string) override;
         void WindowManipulation(DispatchTypes::WindowManipulationType function, VTParameter parameter1, VTParameter parameter2) override;
         void MoveCursor(VTInt row, VTInt col) override;
+        void MoveCursorForHostRequest(VTInt row, VTInt col, uintptr_t target, uint32_t generation, const std::shared_ptr<void>& synchronization) override;
         void FocusChanged(bool focused) override;

     private:
diff --git a/src/terminal/parser/InputStateMachineEngine.cpp b/src/terminal/parser/InputStateMachineEngine.cpp
index 4568232..ff6bbd9 100644
--- a/src/terminal/parser/InputStateMachineEngine.cpp
+++ b/src/terminal/parser/InputStateMachineEngine.cpp
@@ -6,6 +6,8 @@
 #include "stateMachine.hpp"
 #include "InputStateMachineEngine.hpp"

+#include <limits>
+
 #include <til/atomic.h>

 #include "../../inc/unicode.hpp"
@@ -96,9 +98,54 @@ InputStateMachineEngine::InputStateMachineEngine(std::unique_ptr<IInteractDispat
     THROW_HR_IF_NULL(E_INVALIDARG, _pDispatch.get());
 }

-void InputStateMachineEngine::CaptureNextCursorPositionReport() noexcept
+bool InputStateMachineEngine::RegisterHostCursorPositionRequest(std::wstring token, const uintptr_t target, const uint32_t generation, std::shared_ptr<void> synchronization)
 {
-    _captureNextCursorPositionReport.store(true, std::memory_order_relaxed);
+    const std::scoped_lock lock{ _hostCursorPositionMutex };
+    const auto inserted = _hostCursorPositionRequests.emplace(std::move(token), HostCursorPositionRequest{ target, generation, std::move(synchronization) }).second;
+    if (inserted && target == 0)
+    {
+        _startupHostCursorPositionState.store(1, std::memory_order_relaxed);
+    }
+    return inserted;
+}
+
+void InputStateMachineEngine::InvalidateHostCursorPositionRequests(const uintptr_t target) noexcept
+{
+    const std::scoped_lock lock{ _hostCursorPositionMutex };
+    for (auto request = _hostCursorPositionRequests.begin(); request != _hostCursorPositionRequests.end();)
+    {
+        if (request->second.target == target)
+        {
+            request = _hostCursorPositionRequests.erase(request);
+        }
+        else
+        {
+            ++request;
+        }
+    }
+}
+
+void InputStateMachineEngine::CancelHostCursorPositionRequest() noexcept
+{
+    const std::scoped_lock lock{ _hostCursorPositionMutex };
+    _hostCursorPositionRequests.clear();
+    if (_startupHostCursorPositionState.exchange(3, std::memory_order_relaxed) == 1)
+    {
+        til::atomic_notify_all(_startupHostCursorPositionState);
+    }
+}
+
+bool InputStateMachineEngine::WaitUntilStartupHostCursorPositionRequestCompletes() noexcept
+{
+    for (;;)
+    {
+        const auto state = _startupHostCursorPositionState.load(std::memory_order_relaxed);
+        if (state != 1)
+        {
+            return state == 2;
+        }
+        til::atomic_wait(_startupHostCursorPositionState, state, INFINITE);
+    }
 }

 til::enumset<DeviceAttribute, uint64_t> InputStateMachineEngine::WaitUntilDA1(DWORD timeout) noexcept
@@ -122,10 +169,6 @@ til::enumset<DeviceAttribute, uint64_t> InputStateMachineEngine::WaitUntilDA1(DW
         }
     }

-    // VtIo first sends a DSR CPR and then a DA1 request.
-    // If we encountered a DA1 response here, the DSR request is definitely done now.
-    _captureNextCursorPositionReport.store(false, std::memory_order_relaxed);
-
     return til::enumset<DeviceAttribute, uint64_t>::from_bits(val);
 }

@@ -421,14 +464,9 @@ bool InputStateMachineEngine::ActionCsiDispatch(const VTID id, const VTParameter
     }
     // case CsiActionCodes::DSR_DeviceStatusReportResponse:
     case CsiActionCodes::CSI_F3:
-        // The F3 case is special - it shares a code with the DeviceStatusResponse.
-        // If we're looking for that response, then do that, and break out.
-        // Else, fall though to the _GetCursorKeysModifierState handler.
-        if (_captureNextCursorPositionReport.exchange(false, std::memory_order_relaxed))
-        {
-            _pDispatch->MoveCursor(parameters.at(0), parameters.at(1));
-            return true;
-        }
+        // Host-owned cursor synchronization uses the private, request-addressed
+        // OSC handled in ActionOscDispatch. A CSI CPR is therefore always an
+        // application-owned response (or F3), and must never be stolen here.
         // Heuristic: If the hosting terminal used the win32 input mode, chances are high
         // that this is a CPR requested by the terminal application as opposed to a F3 key.
         if (_encounteredWin32InputModeSequence)
@@ -586,8 +624,157 @@ bool InputStateMachineEngine::ActionSs3Dispatch(const wchar_t wch, const VTParam
 // - string - OSC string we've collected. NOT null terminated.
 // Return Value:
 // - true if we handled the dispatch.
-bool InputStateMachineEngine::ActionOscDispatch(const size_t /*parameter*/, const std::wstring_view /*string*/) noexcept
+bool InputStateMachineEngine::ActionOscDispatch(const size_t parameter, const std::wstring_view string) noexcept
 {
+    static constexpr size_t termwrightPrivateOsc = 8488;
+    static constexpr std::wstring_view hostCursorPrefix{ L"twh-cpr-v1:r:" };
+    static constexpr std::wstring_view applicationReplyPrefix{ L"twh-app-reply-v1:" };
+
+    if (parameter == termwrightPrivateOsc && string.starts_with(hostCursorPrefix))
+    {
+        const auto tokenBegin = hostCursorPrefix.size();
+        const auto rowSeparator = string.find(L':', tokenBegin);
+        const auto colSeparator = rowSeparator == std::wstring_view::npos ? rowSeparator : string.find(L':', rowSeparator + 1);
+        if (rowSeparator != std::wstring_view::npos && colSeparator != std::wstring_view::npos && string.find(L':', colSeparator + 1) == std::wstring_view::npos)
+        {
+            const auto token = string.substr(tokenBegin, rowSeparator - tokenBegin);
+            const auto parseCoordinate = [](const std::wstring_view value, VTInt& result) noexcept {
+                if (value.empty())
+                {
+                    return false;
+                }
+                uint64_t parsed = 0;
+                for (const auto ch : value)
+                {
+                    if (ch < L'0' || ch > L'9')
+                    {
+                        return false;
+                    }
+                    const auto digit = static_cast<uint64_t>(ch - L'0');
+                    const auto maximum = static_cast<uint64_t>(std::numeric_limits<VTInt>::max());
+                    if (parsed > (maximum - digit) / 10)
+                    {
+                        return false;
+                    }
+                    parsed = parsed * 10 + digit;
+                }
+                if (parsed == 0)
+                {
+                    return false;
+                }
+                result = static_cast<VTInt>(parsed);
+                return true;
+            };
+
+            VTInt row = 0;
+            VTInt col = 0;
+            if (token.size() == 32 && parseCoordinate(string.substr(rowSeparator + 1, colSeparator - rowSeparator - 1), row) && parseCoordinate(string.substr(colSeparator + 1), col))
+            {
+                std::optional<HostCursorPositionRequest> request;
+                {
+                    const std::scoped_lock lock{ _hostCursorPositionMutex };
+                    const auto found = _hostCursorPositionRequests.find(std::wstring{ token });
+                    if (found != _hostCursorPositionRequests.end())
+                    {
+                        request = found->second;
+                        for (auto candidate = _hostCursorPositionRequests.begin(); candidate != _hostCursorPositionRequests.end();)
+                        {
+                            if (candidate->second.target == request->target)
+                            {
+                                candidate = _hostCursorPositionRequests.erase(candidate);
+                            }
+                            else
+                            {
+                                ++candidate;
+                            }
+                        }
+                    }
+                }
+                if (request)
+                {
+                    _pDispatch->MoveCursorForHostRequest(row, col, request->target, request->generation, request->synchronization);
+                    if (request->target == 0)
+                    {
+                        _startupHostCursorPositionState.store(2, std::memory_order_relaxed);
+                        til::atomic_notify_all(_startupHostCursorPositionState);
+                    }
+                }
+            }
+        }
+
+        // OSC 8488 with this versioned response prefix is reserved for the
+        // host RPC. Consume stale, superseded and malformed replies without
+        // letting host-control traffic leak into the application input.
+        return true;
+    }
+
+    if (parameter == termwrightPrivateOsc && string.starts_with(applicationReplyPrefix))
+    {
+        static constexpr size_t maximumReplyBytes = 4096;
+        const auto lengthBegin = applicationReplyPrefix.size();
+        const auto lengthSeparator = string.find(L':', lengthBegin);
+        size_t expectedBytes = 0;
+        bool valid = lengthSeparator != std::wstring_view::npos && lengthSeparator > lengthBegin;
+        if (valid)
+        {
+            for (const auto ch : string.substr(lengthBegin, lengthSeparator - lengthBegin))
+            {
+                if (ch < L'0' || ch > L'9')
+                {
+                    valid = false;
+                    break;
+                }
+                const auto digit = static_cast<size_t>(ch - L'0');
+                if (expectedBytes > (maximumReplyBytes - digit) / 10)
+                {
+                    valid = false;
+                    break;
+                }
+                expectedBytes = expectedBytes * 10 + digit;
+            }
+        }
+
+        const auto encoded = lengthSeparator == std::wstring_view::npos ? std::wstring_view{} : string.substr(lengthSeparator + 1);
+        valid = valid && expectedBytes > 0 && expectedBytes <= maximumReplyBytes && encoded.size() == expectedBytes * 2;
+        std::wstring decoded;
+        if (valid)
+        {
+            decoded.reserve(expectedBytes);
+            const auto hexDigit = [](const wchar_t ch) noexcept -> int {
+                if (ch >= L'0' && ch <= L'9')
+                {
+                    return ch - L'0';
+                }
+                if (ch >= L'a' && ch <= L'f')
+                {
+                    return ch - L'a' + 10;
+                }
+                return -1;
+            };
+            for (size_t index = 0; index < encoded.size(); index += 2)
+            {
+                const auto high = hexDigit(encoded[index]);
+                const auto low = hexDigit(encoded[index + 1]);
+                const auto byte = high < 0 || low < 0 ? 0x100 : (high << 4) | low;
+                if (byte > 0x7f)
+                {
+                    valid = false;
+                    break;
+                }
+                decoded.push_back(static_cast<wchar_t>(byte));
+            }
+        }
+        if (valid)
+        {
+            // WriteStringRaw bypasses CSI interpretation and commits every
+            // response byte under one InputBuffer wakeup in either input mode.
+            _pDispatch->WriteStringRaw(decoded);
+        }
+
+        // The versioned namespace is reserved. Malformed, oversized and
+        // non-ASCII envelopes are consumed fail-closed without input leakage.
+        return true;
+    }
     // Unlike ActionCsiDispatch, we are not checking whether the application has requested
     // VT input.
     // Our documentation states that VT reports generated by application requests will be
diff --git a/src/terminal/parser/InputStateMachineEngine.hpp b/src/terminal/parser/InputStateMachineEngine.hpp
index e0c7eec..d1e6d24 100644
--- a/src/terminal/parser/InputStateMachineEngine.hpp
+++ b/src/terminal/parser/InputStateMachineEngine.hpp
@@ -17,6 +17,8 @@ Author(s):

 #include "IStateMachineEngine.hpp"
 #include <functional>
+#include <mutex>
+#include <unordered_map>
 #include "../../types/inc/IInputEvent.hpp"
 #include "../adapter/IInteractDispatch.hpp"

@@ -163,7 +165,10 @@ namespace Microsoft::Console::VirtualTerminal
     public:
         InputStateMachineEngine(std::unique_ptr<IInteractDispatch> pDispatch);

-        void CaptureNextCursorPositionReport() noexcept;
+        bool RegisterHostCursorPositionRequest(std::wstring token, uintptr_t target, uint32_t generation, std::shared_ptr<void> synchronization);
+        void InvalidateHostCursorPositionRequests(uintptr_t target) noexcept;
+        void CancelHostCursorPositionRequest() noexcept;
+        bool WaitUntilStartupHostCursorPositionRequestCompletes() noexcept;
         til::enumset<DeviceAttribute, uint64_t> WaitUntilDA1(DWORD timeout) noexcept;

         void UnknownSequence() noexcept override;
@@ -193,7 +198,15 @@ namespace Microsoft::Console::VirtualTerminal
     private:
         const std::unique_ptr<IInteractDispatch> _pDispatch;
         std::atomic<uint64_t> _deviceAttributes{ 0 };
-        std::atomic<bool> _captureNextCursorPositionReport{ false };
+        std::mutex _hostCursorPositionMutex;
+        struct HostCursorPositionRequest
+        {
+            uintptr_t target;
+            uint32_t generation;
+            std::shared_ptr<void> synchronization;
+        };
+        std::unordered_map<std::wstring, HostCursorPositionRequest> _hostCursorPositionRequests;
+        std::atomic<uint8_t> _startupHostCursorPositionState{ 0 };
         bool _encounteredWin32InputModeSequence = false;
         bool _expectingStringTerminator = false;
         DWORD _mouseButtonState = 0;
diff --git a/src/terminal/parser/ut_parser/InputEngineTest.cpp b/src/terminal/parser/ut_parser/InputEngineTest.cpp
index 19aa939..f94a5df 100644
--- a/src/terminal/parser/ut_parser/InputEngineTest.cpp
+++ b/src/terminal/parser/ut_parser/InputEngineTest.cpp
@@ -252,6 +252,7 @@ class Microsoft::Console::VirtualTerminal::InputEngineTest
     TEST_METHOD(RoundTripTest);
     TEST_METHOD(NonAsciiTest);
     TEST_METHOD(CursorPositioningTest);
+    TEST_METHOD(TermwrightApplicationReplyEnvelopeTest);
     TEST_METHOD(CSICursorBackTabTest);
     TEST_METHOD(EnhancedKeysTest);
     TEST_METHOD(SS3CursorKeyTest);
@@ -680,25 +680,15 @@ void InputEngineTest::CursorPositioningTest()

     auto dispatch = std::make_unique<TestInteractDispatch>(pfn, &testState);
     auto inputEngine = std::make_unique<InputStateMachineEngine>(std::move(dispatch));
-    inputEngine->CaptureNextCursorPositionReport();
     auto _stateMachine = std::make_unique<StateMachine>(std::move(inputEngine));
     testState._stateMachine = _stateMachine.get();

     Log::Comment(NoThrowString().Format(
-        L"Try sending a cursor position response, then send it again. "
-        L"The first time, it should be interpreted as a cursor position. "
-        L"The state machine engine should reset itself to normal operation "
-        L"after that, and treat the second as an F3."));
+        L"A standard cursor position response belongs to the application. "
+        L"Host-owned cursor synchronization uses the request-addressed "
+        L"OSC 8488 protocol instead, so both reports are treated as F3."));

     std::wstring seq = L"\x1b[1;4R";
-    testState._expectCursorPosition = true;
-    testState._expectedCursor = { 4, 1 };
-
-    Log::Comment(NoThrowString().Format(
-        L"Processing \"%s\"", seq.c_str()));
-    _stateMachine->ProcessString(seq);
-
-    testState._expectCursorPosition = false;

     INPUT_RECORD inputRec;
     inputRec.EventType = KEY_EVENT;
@@ -709,6 +699,11 @@ void InputEngineTest::CursorPositioningTest()
     inputRec.Event.KeyEvent.wVirtualScanCode = static_cast<WORD>(OneCoreSafeMapVirtualKeyW(VK_F3, MAPVK_VK_TO_VSC));
     inputRec.Event.KeyEvent.uChar.UnicodeChar = L'\0';

+    Log::Comment(NoThrowString().Format(
+        L"Processing \"%s\"", seq.c_str()));
+    testState.vExpectedInput.push_back(inputRec);
+    _stateMachine->ProcessString(seq);
+
     testState.vExpectedInput.push_back(inputRec);
     Log::Comment(NoThrowString().Format(
         L"Processing \"%s\"", seq.c_str()));
@@ -716,6 +712,65 @@ void InputEngineTest::CursorPositioningTest()
     VerifyExpectedInputDrained();
 }
-
+
+void InputEngineTest::TermwrightApplicationReplyEnvelopeTest()
+{
+    const auto verifyEnvelope = [](const std::wstring_view envelope, const std::wstring_view expected) {
+        for (size_t split = 0; split < envelope.size(); ++split)
+        {
+            size_t writes = 0;
+            std::wstring actual;
+            const auto callback = [&](const std::span<const INPUT_RECORD>& records) {
+                ++writes;
+                for (const auto& record : records)
+                {
+                    VERIFY_ARE_EQUAL(KEY_EVENT, record.EventType);
+                    VERIFY_IS_TRUE(record.Event.KeyEvent.bKeyDown);
+                    actual.push_back(record.Event.KeyEvent.uChar.UnicodeChar);
+                }
+            };
+            auto dispatch = std::make_unique<TestInteractDispatch>(callback, nullptr);
+            auto inputEngine = std::make_unique<InputStateMachineEngine>(std::move(dispatch));
+            auto stateMachine = std::make_unique<StateMachine>(std::move(inputEngine));
+
+            stateMachine->ProcessString(envelope.substr(0, split));
+            VERIFY_ARE_EQUAL(0u, writes);
+            stateMachine->ProcessString(envelope.substr(split));
+            VERIFY_ARE_EQUAL(1u, writes);
+            VERIFY_ARE_EQUAL(std::wstring{ expected }, actual);
+        }
+    };
+
+    verifyEnvelope(L"\x1b]8488;twh-app-reply-v1:7:1b5b393b313752\x07", L"\x1b[9;17R");
+    verifyEnvelope(L"\x1b]8488;twh-app-reply-v1:11:1b5b3f323032363b322479\x07", L"\x1b[?2026;2$y");
+
+    for (const auto malformed : {
+             L"\x1b]8488;twh-app-reply-v1:0:\x07",
+             L"\x1b]8488;twh-app-reply-v1:2:1b\x07",
+             L"\x1b]8488;twh-app-reply-v1:1:zz\x07",
+             L"\x1b]8488;twh-app-reply-v1:1:80\x07",
+             L"\x1b]8488;twh-app-reply-v1:4097:00\x07",
+             L"\x1b]8488;twh-app-reply-v1:999999999999999999999999:00\x07",
+         })
+    {
+        size_t writes = 0;
+        const auto callback = [&](const std::span<const INPUT_RECORD>&) { ++writes; };
+        auto dispatch = std::make_unique<TestInteractDispatch>(callback, nullptr);
+        auto inputEngine = std::make_unique<InputStateMachineEngine>(std::move(dispatch));
+        auto stateMachine = std::make_unique<StateMachine>(std::move(inputEngine));
+        stateMachine->ProcessString(malformed.substr(0, malformed.size() - 1));
+        VERIFY_ARE_EQUAL(0u, writes);
+        stateMachine->ProcessString(malformed.substr(malformed.size() - 1));
+        VERIFY_ARE_EQUAL(0u, writes);
+    }
+
+    size_t writes = 0;
+    const auto callback = [&](const std::span<const INPUT_RECORD>&) { ++writes; };
+    auto dispatch = std::make_unique<TestInteractDispatch>(callback, nullptr);
+    auto inputEngine = std::make_unique<InputStateMachineEngine>(std::move(dispatch));
+    VERIFY_IS_FALSE(inputEngine->ActionOscDispatch(8488, L"application-owned"));
+    VERIFY_ARE_EQUAL(0u, writes);
+}
+
 void InputEngineTest::CSICursorBackTabTest()
 {
     auto pfn = std::bind(&TestState::TestInputCallback, &testState, std::placeholders::_1);
diff --git a/dep/vcpkg-overlay-triplets/arm64-windows-static.cmake b/dep/vcpkg-overlay-triplets/arm64-windows-static.cmake
--- a/dep/vcpkg-overlay-triplets/arm64-windows-static.cmake
+++ b/dep/vcpkg-overlay-triplets/arm64-windows-static.cmake
@@ -6,3 +6,3 @@ set(VCPKG_LIBRARY_LINKAGE static)
 # ...but with explicit platform toolset, so that future toolsets
 # aren't automatically picked up (it defaults to the latest one).
-set(VCPKG_PLATFORM_TOOLSET v145)
+set(VCPKG_PLATFORM_TOOLSET v143)
diff --git a/dep/vcpkg-overlay-triplets/x64-windows-static.cmake b/dep/vcpkg-overlay-triplets/x64-windows-static.cmake
--- a/dep/vcpkg-overlay-triplets/x64-windows-static.cmake
+++ b/dep/vcpkg-overlay-triplets/x64-windows-static.cmake
@@ -6,3 +6,3 @@ set(VCPKG_LIBRARY_LINKAGE static)
 # ...but with explicit platform toolset, so that future toolsets
 # aren't automatically picked up (it defaults to the latest one).
-set(VCPKG_PLATFORM_TOOLSET v145)
+set(VCPKG_PLATFORM_TOOLSET v143)
