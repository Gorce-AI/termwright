---
'termwright': patch
---

Allow injected Native Host engines to provide explicit Git provenance while production hosts continue automatic capture. Host lifecycle tests now clean up causally without depending on Windows filesystem or process latency.
