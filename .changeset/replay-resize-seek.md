---
'@termwright/ui': patch
---

Replay terminal output and resize frames in their recorded order so direct seeks, hover previews, and incremental playback render the same screen. Keep the displayed grid size, semantic picking, and terminal highlights aligned with resize events at the selected moment.
