# @termwright/test snapshots — generated, but review them like test source.
# Update with `vitest -u` or TERMWRIGHT_UPDATE_SNAPSHOTS=changed.
"publishes the notebook as a semantic tree 1": |
  - application "NotesApp":
      - region:
          - text "Notes"
          - list "notes" [focused]:
              - listitem "buy milk":
                  - text "buy milk"
              - listitem "call the bank":
                  - text "call the bank"
              - listitem "write the release notes":
                  - text "write the release notes"
          - textbox "New note"
          - region:
              - button "Add"
              - button "Delete"
          - 'text "status: ready"'
